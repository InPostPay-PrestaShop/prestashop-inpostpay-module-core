const BACKEND_AJAX_URL = window.inpostizi_backend_ajax_url;
const CART_AJAX_URL = window.inpostizi_cart_ajax_url;

export const getBackendUrl = () => {
  return BACKEND_AJAX_URL;
};

export const getCartUrl = () => {
  return CART_AJAX_URL;
};
