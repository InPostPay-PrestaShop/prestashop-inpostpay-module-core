{# TODO: CHANGE THIS, IT'T TEMPORARY SOLUTION #}

<inpost-izi-button
  binding_place="PRODUCT_CARD"
  class="float-{{ btnConfig.alignment ? btnConfig.alignment.jsonSerialize() : '' }}"
  variant="{{ btnConfig.variant ? btnConfig.variant.jsonSerialize() : '' }}"
  dark_mode="{{ btnConfig.darkMode }}"
  max_width="{{ btnConfig.maxWidthPx }}"
  min_width="{{ btnConfig.minWidthPx }}"
  frame_style="{{ btnConfig.frameStyle ? btnConfig.frameStyle.jsonSerialize() : '' }}"
  language="pl"
  class="float-center"
  bindinginprogress="false"
  baskedlinked="false">
</inpost-izi-button>
